This is the main script that orchestrates the entire program. When the green flag is clicked, it erases the stage and initializes the count variable. The when stage clicked event triggers the drawing process: it moves the pen to the mouse's position, calls the chooseShape function to draw the correct polygon, and then increments the count variable, resetting it to 1 if it goes past 3.