Here, I have updated the quiz generation now using llama3.1 model, which has unlimited token access (almost)
And, to resolve Token per minute issues the code has delays inserted in case TPM is hit
Also, interactive user input to get the grade of the user and generate quiz accordingly